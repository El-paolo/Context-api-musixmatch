import React from 'react';
import { Paper } from '@mui/material';
import { TextField } from '@mui/material';


const SearchTracks = ({ SearchTracks }) => {
  return (
    <div>SearchTracks</div>
  )
}

export default SearchTracks